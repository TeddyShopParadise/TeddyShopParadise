@model IEnumerable<TeddyShop.Models.Vendedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoVendedor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DniEmpleadoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoVendedor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DniEmpleadoNavigation.DniEmpleado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DniEmpleado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DniEmpleado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DniEmpleado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
